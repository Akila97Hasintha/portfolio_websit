export default function HeroSection({ scrollY }: { scrollY: number }) {
  return (
    <section id="hero" className="min-h-screen flex items-center">
      <h1 className="text-7xl font-bold">Software Engineer</h1>
    </section>
  );
}
